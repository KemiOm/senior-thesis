# Virtual environment
venv/
.venv/
env/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.eggs/
dist/
build/

# Fine-tuning checkpoints (large; keep on disk / cluster; regenerate or rsync).
# sft/ = default --output-root; sft_full/ = optional alternate name only (same ignore rules).
sft/**/checkpoint-*/
sft/**/final_model/
sft/**/final_model_merged/
sft_full/**/checkpoint-*/
sft_full/**/final_model/
sft_full/**/final_model_merged/
# sft_runs/ = experiment registry; keep lightweight metadata, ignore heavy weight artifacts.
sft_runs/**/checkpoint-*/
sft_runs/**/final_model/
sft_runs/**/final_model_merged/
sft_runs/**/*.safetensors
sft_runs/**/training_args.bin
sft_runs/**/optimizer.pt
sft_runs/**/scheduler.pt
sft_runs/**/rng_state*.pth

# Rsync / HF temp partials next to merged weights
**/.model.safetensors.*
**/.cache/huggingface/

# Notebook-generated figures (regenerate with visualizations/plot_ft.py)
visualizations/out/

# Output: keep most artifacts local; version only prepared training JSON + corpus for advisor review.
output/*
!output/corpus.db
!output/training_data/
!output/training_data/**

data/nltk_data/

# Prompt baseline JSON outputs are large but versioned for thesis / advisor review.
# (Regenerate with scripts/run_prompt_eval.py + evaluation/summarize_prompt_baselines.py.)

# ECPA source corpus (external dataset; obtain separately)
ECPA/

# IDE and OS
.idea/
.vscode/
.DS_Store

# Logs and temp
*.log
*.tmp
