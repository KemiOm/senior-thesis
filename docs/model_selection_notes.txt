# Model selection based off of prompt-only baselines

Source: `evaluation/results/model_comparison.csv`

Exact match = percentage of lines where `model_output` equals `gold_target` (after strip; `natural_text` collapses whitespace). Structured partial columns (`st_*`)

Primary prompt focus below: **`few_shot`**. `model_comparison.csv` lists all prompt types per model.

---

## Best model per task (`few_shot`, test split)

Numbers are from the current `model_comparison.csv` in this repo.

| Task | Best on exact match | Score | Notes |
|------|---------------------|-------|--------|
| **meter_only** | **google/flan-t5-large** | **34.80%** | Highest `exact_match_pct` among all models in the table for this task/prompt. |
| **rhyme_only** | *(no exact-match winner)* | 0% exact (all tied) | Best **relaxed** rhyme token match (`st_rhyme_relaxed_match_pct`): **google/flan-t5-small** at **0.276%** others 0.0%|
| **natural_text** | *(no winner)* | **0%** for every model | Full-line exact match to gold text; all models tied at zero in this export. |
| **combined** | *(no exact-match winner)* | 0% exact (all tied) | Best **mean field** agreement on parsed bundle (`st_combined_mean_field_pct`): **gpt2** at **29.6%** gpt2-medium 29.5%, gpt2-large 22.75%, phi-2 14.1%

**takeaway:** **FLAN-T5-large** is the clear choice for **meter_only** under few-shot. Other tasks do not separate models on **exact** match in this table; secondary metrics (rhyme relaxed, combined fields) point to **flan-t5-small** vs **GPT-2** depending on task.

---

## Best mean exact-match across the four tasks (`few_shot`)

Average of `exact_match_pct` over meter_only, natural_text, rhyme_only, combined 

| Model | Mean exact % |
|-------|----------------|
| **google/flan-t5-large** | **8.70%** |
| google/flan-t5-small | 1.21% |
| google/flan-t5-base | 0.00% |
| facebook/bart-base | 0.00% |
| facebook/bart-large | 0.00% |
| gpt2 | 0.00% |
| gpt2-large | 0.00% |
| gpt2-medium | 0.00% |


